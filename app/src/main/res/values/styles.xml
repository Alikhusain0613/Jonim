<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Card style -->
    <style name="Widget.Jonim.Card" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/surface</item>
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">4dp</item>
    </style>

    <!-- Toolbar style -->
    <style name="Widget.Jonim.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">@color/teal_primary_dark</item>
        <item name="android:textColor">@color/chippy_text</item>
    </style>
</resources>